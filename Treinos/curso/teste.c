#include <stdio.h>
#include <stdbool.h>

int main(){

	int x = 10;
	
	char strValue[10] = "25";

/*	printf("%ld\n", sizeof(bool) );

	sprintf(strValue, "%d", x);

	printf("%s\n",strValue);
*/
	printf("%d\n", atoi(strValue) );

	return 0;
}
